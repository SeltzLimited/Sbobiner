#!/usr/bin/env bash
# Installazione: doppio click da Finder. Serve internet solo adesso.
cd "$(dirname "$0")"
./setup.sh
echo
echo "Puoi chiudere questa finestra. Per usare il programma: doppio click su start.command"
echo "(premi Invio per chiudere)"
read -r
